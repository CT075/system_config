export SSH_AUTH_SOCK="$XDG_RUNTIME_DIR/ssh-agent.socket"

MODE_CURSOR_VIINS="steady bar"
MODE_CURSOR_REPLACE="steady underline"
MODE_CURSOR_SEARCH="#ff00ff steady underline"
bindkey -v
source "$HOME/System/zsh-vi-mode/zsh-vi-mode.plugin.zsh"
bindkey -rpM viins '^[^['

export EDITOR=nvim
export VISUAL=nvim
export PATH=$PATH:/home/cam/.local/bin

[ -f "/home/cam/.ghcup/env" ] && source "/home/cam/.ghcup/env" # ghcup-env

export DEVKITPRO=/opt/devkitpro
export DEVKITARM=${DEVKITPRO}/devkitARM
export DEVKITPPC=${DEVKITPRO}/devkitPPC

export PATH=$PATH:${DEVKITPRO}/devkitARM/bin
export FZF_DEFAULT_COMMAND='rg --files --hidden'

export PATH=$PATH:$HOME/.idris2/bin
export PATH=$PATH:$HOME/.pack/bin
export PATH=$PATH:$HOME/go/bin
export PATH=$PATH:$HOME/.elan/bin

autoload -Uz compinit
compinit
alias in='task add +in'

[ "$TERM" = "alacritty" ] && alias ssh='TERM=xterm-256color ssh'

eval "$(starship init zsh)"
