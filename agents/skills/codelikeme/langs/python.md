# codelikeme python conventions

This file contains some additional conventions that the user prefers when writing Python code.

- Prefer list comprehensions to `map` and `reduce`.
  - Do not nest more than one comprehension.
